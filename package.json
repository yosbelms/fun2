{
  "name": "fun2",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f *.js *.js.map",
    "build": "npm run clean && tsc -p ./tsconfig.browser.json && tsc -p ./tsconfig.nodejs.json",
    "watch": "npx sane 'npm run build' --glob='*.ts'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/deep-freeze": "^0.1.2",
    "@types/node": "^12.7.5",
    "deep-freeze": "^0.0.1",
    "p-defer": "^3.0.0",
    "p-timeout": "^3.2.0"
  },
  "devDependencies": {
    "jasmine": "^3.5.0",
    "jasmine-ts": "^0.3.0",
    "kckr": "^3.0.0",
    "sane": "^4.1.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.3"
  }
}
